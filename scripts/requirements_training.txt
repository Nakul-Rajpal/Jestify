# Jestify Finetuning & Training Dependencies
# Install with: pip install -r scripts/requirements_training.txt
#
# For local GPU (Quadro RTX 5000 / RTX 5070):
#   pip install -r scripts/requirements_training.txt
#
# For AWS EC2 (pre-installed PyTorch AMI — skip torch/cuda lines):
#   pip install -r scripts/requirements_training.txt --no-deps
#   pip install transformers peft trl accelerate datasets bitsandbytes

# ── HuggingFace training stack ────────────────────────────────────────────────
transformers>=4.45.0
datasets>=2.20.0
peft>=0.12.0
trl>=0.11.0
accelerate>=0.33.0

# ── Quantization (QLoRA 4-bit) ────────────────────────────────────────────────
bitsandbytes>=0.43.0

# ── PyTorch (CUDA 12.x for RTX 5000 / RTX 5070) ──────────────────────────────
# Install via: pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu124
# (Do NOT install via requirements.txt — version must match your CUDA)
# torch>=2.3.0

# ── Data & utilities ──────────────────────────────────────────────────────────
huggingface_hub>=0.24.0
safetensors>=0.4.3
sentencepiece>=0.2.0          # tokenizer for Qwen models
tokenizers>=0.19.0

# ── Experiment tracking ───────────────────────────────────────────────────────
tensorboard>=2.17.0

# ── Data processing ───────────────────────────────────────────────────────────
numpy>=1.26.0
python-dotenv>=1.0.0

# ── AWS Bedrock (for dataset generation with AWS credits) ─────────────────────
# boto3>=1.34.0
# anthropic[bedrock]>=0.34.0

# ── GGUF conversion (needs llama.cpp built separately — see convert_to_gguf.sh)
# No pip package needed — use llama.cpp Python scripts directly
